# =============================================================================
# Orca AUV —— 全系統環境設定的唯一來源
# =============================================================================
# docker compose 會自動讀取本檔案，並注入所有堆疊的容器。
#
# 這是「單一來源」的意思：控制堆疊、感知決策堆疊、模擬三者的 ROS/DDS 設定
# 全部從這裡來。不要在任何子 repo 的 compose 或 Makefile 裡另外寫死這些值。
#
# 子 repo 各自也有一份 .env，那是給「單獨開發該堆疊」用的 fallback。
# 從 super-repo 啟動時，compose 讀的是本檔案，子 repo 的那份不會生效。
#
# 要在本機覆寫又不進版控，建立 .env.local 並用：
#   docker compose --env-file .env --env-file .env.local ...
# -----------------------------------------------------------------------------

# --- ROS 2 網路 -------------------------------------------------------------
ROS_DOMAIN_ID=0
ROS_LOCALHOST_ONLY=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp

# 關閉 Fast DDS 的共享記憶體傳輸，只走 UDPv4。
#
# 這是正確性設定，不是效能調校，不要為了「SHM 比較快」把它拿掉。
#
# 控制堆疊的容器掛載了 host 的 /dev（為了存取 STM32 序列埠），連帶把
# /dev/shm 也換成 host 的；感知與模擬容器則各自有獨立的 /dev/shm。
# Fast DDS 預設會宣告 SHM locator，此時跨容器的 participant 會互相 match 到、
# 資料卻永遠走不通，而且不會有任何錯誤訊息 —— 表現出來就是「載具不動」。
#
# 實測：未設定時，容器內 `ros2 node list` 連自己啟動的節點都看不到；
# 設定後三個容器 19 個節點一次到齊。
FASTDDS_BUILTIN_TRANSPORTS=UDPv4

# --- 載具 -------------------------------------------------------------------
# 所有 topic 的 namespace 前綴。
# 注意：SAUVC-JETSON 的 decision.launch.py 目前把 remap 目標硬編碼成
# /orca_auv/...，改這個值會讓決策層靜默失效（wrench_sum 的 timeout 把該來源
# 歸零，載具只是不動、沒有錯誤訊息）。改之前請一併修那邊。
ORCA_NAMESPACE=orca_auv

# --- 裝置路徑 ---------------------------------------------------------------
# 一律優先用 /dev/serial/by-id/ 穩定路徑：STM32 從 RPi 換到 Jetson 之後
# /dev/ttyUSB* 的編號不保證相同，插拔順序也會影響。查法：
#   ls -l /dev/serial/by-id/
ORCA_STM32_PORT=/dev/ttyUSB0

# --- 映像 -------------------------------------------------------------------
# 控制堆疊：由本 compose 直接 build。
ORCA_CONTROL_IMAGE=dianyueguo/orca-auv-rpi-ros2-image:latest

# 感知決策堆疊：Isaac ROS 映像不由 compose build（它由 isaac_ros_common 的
# build_image_layers.sh 分層組出來）。這裡填已經存在於本機的映像名稱。
#
# 取得方式二選一：
#   a) 從 registry 拉（建議，Jetson 上請用這個）：
#        SAUVC-JETSON/isaac_ros_common/scripts/orca_registry.sh pull
#   b) 本機自建（第一次約 40 分鐘以上）：
#        cd SAUVC-JETSON/isaac_ros_common && ./scripts/run_dev.sh
ORCA_AUTONOMY_IMAGE=isaac_ros_dev-x86_64:latest

# 模擬。
ORCA_SIM_IMAGE=orca-auv-gazebo-simulation-image:latest

# --- Bag 錄製 ---------------------------------------------------------------
# bag 落地在 host 的這個目錄（已加入 .gitignore）。
ORCA_BAG_DIR=./bags

# --- X11（GUI / rqt / Gazebo GUI）-------------------------------------------
HOST_DISPLAY=:0
XAUTH_FILE=/tmp/.Xauthority
XAUTHORITY=/tmp/.Xauthority
